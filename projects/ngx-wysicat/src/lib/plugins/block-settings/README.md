# Block Hover Menu

Responsible for displaying the block menu next to the hovered block (line).
Inside, there is an `<ng-content>` element where an `add-block` icon or a `drag-and-drop` icon can be placed.
